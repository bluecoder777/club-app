services:
  backend:
    build: 
      context: backend
      dockerfile: Dockerfile
    container_name: club-be
    ports:
      - "8080:8080"
  frontend:
    build: 
      context: frontend
      dockerfile: Dockerfile
    container_name: club-fe
    ports:
      - "80:80"
